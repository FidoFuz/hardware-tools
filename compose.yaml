services:
  hardware-tools:
    container_name: hardware-tools
    build:
      context: .
      args:
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_DATE: ${BUILD_DATE:-unknown}
        IMAGE_NAME: hardware-tools